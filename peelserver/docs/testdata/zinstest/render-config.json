{
  "renderConfigurations": {
    "VARIABLE_NAME": "{{valueText}}",
    "LITERAL": "{{ valueText }}",
    "IF_STATEMENT": "if ({% for condition in conditions %}{{ condition | renderTraceExpression }}{% if not loop.last %} || {% endif %}{% endfor %}){% if executedBlock != null %} {{ executedBlock | renderTraceExpression }}{% endif %}",
    "BLOCK": "{{ '{' }}{% for statement in content %}{{ statement | renderTraceExpression }}{% if not loop.last %}; {% endif %}{% endfor %}{{ '}' }}",
    "ASSIGNMENT": "{{ variableName }} = {{ expression | renderTraceExpression }}",
    "FOR_EACH_LOOP": "for ({{ variableName }} in {{ iterableExpression | renderTraceExpression }}) {% if iterations|length > 0 and iterations[0].body != null %}{{ iterations[0].body | renderTraceExpression }}{% else %}{{ '{' }}{{ '}' }}{% endif %}",
    "RETURN_EXPR": "return {{ expression | renderTraceExpression }}",
    "BINARY_OPERATOR": "{{  lhs | renderTraceExpression }} {{ operator }} {{ rhs | renderTraceExpression }}",
    "LIST_LITERAL": "[{% for element in elements %}{{ element | renderTraceExpression }}{% if not loop.last %}, {% endif %}{% endfor %}]",
    "WHILE_LOOP": "while ({% if iterations|length > 0 and iterations[0].condition != null %}{{ iterations[0].condition | renderTraceExpression }}{% else %}...{% endif %}) {% if iterations|length > 0 and iterations[0].body != null %}{{ iterations[0].body | renderTraceExpression }}{% else %}{{ '{' }}{{ '}' }}{% endif %}",
    "UNARY_PREFIX_OPERATOR": "{{ operator }}{{ argument | renderTraceExpression }}",
    "MAP_LITERAL": "{{ '{' }}{% for entry in entries %}{{ entry.key | renderTraceExpression }}: {{ entry.value | renderTraceExpression }}{% if not loop.last %}, {% endif %}{% endfor %}{{ '}' }}",
    "SELECTOR": "{{ target | renderTraceExpression }}[{{ selector | renderTraceExpression }}]",
    "FUNCTION_CALL": "{{name}}({% for argument in arguments %}{{argument.value.value}}{% if not loop.last %}, {% endif %}{% endfor %})"
  },
  "namedOverrides": {
    "berechnungUndWert": {
      "ASSIGNMENT": "{{ variableName }}  = [{{valueText}}]  = {{ expression | renderTraceExpression }}"
    },
    "nurWert": {
      "ASSIGNMENT": "{{valueText}}"
    }
  }
}